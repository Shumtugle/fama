<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Ground" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/ground</item>
        <item name="android:colorBackground">@color/ground</item>
        <item name="android:statusBarColor">@color/ground</item>
        <item name="android:navigationBarColor">@color/ground</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- Our own handles for picked words. -->
        <item name="android:textSelectHandle">@drawable/caret_mid</item>
        <item name="android:textSelectHandleLeft">@drawable/caret_left</item>
        <item name="android:textSelectHandleRight">@drawable/caret_right</item>
        <item name="android:colorControlActivated">#FFFFA893</item>
        <item name="android:colorAccent">#FFFFA893</item>
    </style>

    <!-- The sheet that rises over whatever shared a channel: the window is
         glass, and the sheet draws its own dimming and its own arrival. -->
    <style name="Veil" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:colorAccent">#FFFFA893</item>
    </style>
</resources>
